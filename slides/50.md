##  Examples of Short Circuit Operators


<code>
String name = "hello";
if (name != null && name.length() > 0)
    System.out.println(name.toUpperCase());

</code>

note:
    logical operators stop operation based on logical comparisons
